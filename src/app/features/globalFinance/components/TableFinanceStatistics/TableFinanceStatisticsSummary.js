import React from 'react';
import PropTypes from 'prop-types';

export function TableFinanceStatisticsSummary({ children }) {
	return <>{children}</>;
}
TableFinanceStatisticsSummary.propTypes = {
	children: PropTypes.node.isRequired
};
