import React from 'react';
import PropTypes from 'prop-types';

export function TableFinanceStatisticsDetails({ children }) {
	return <>{children}</>;
}
TableFinanceStatisticsDetails.propTypes = {
	children: PropTypes.node.isRequired
};
